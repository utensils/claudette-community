# Slash commands

> Placeholder. The contribution flow for slash commands is not yet finalized — track the registry feature issue in the Claudette repo for status.

Slash commands today are loaded from disk by Claudette via `src/slash_commands.rs`. A community-shareable format for them will land alongside the registry feature.

If you have a slash command you'd like to share now, file an issue using the **New Plugin** template and pick "slash command" — we'll capture the request and revisit when the schema is settled.
