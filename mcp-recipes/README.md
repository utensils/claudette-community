# MCP recipes

> Placeholder. The contribution flow for MCP server presets is not yet finalized — track the registry feature issue in the Claudette repo for status.

Claudette manages MCP server lifecycles via `src/mcp.rs` / `src/mcp_supervisor.rs`. A shareable "recipe" format (preset config + setup notes) will land alongside the registry feature.

If you have an MCP server config worth sharing now, file an issue using the **New Plugin** template and pick "MCP recipe" — we'll capture the request.
